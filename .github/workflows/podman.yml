name: Podman

# Only run on branches (e.g. not tags)
on:
  workflow_dispatch:

jobs:
  heml:
    name: setp
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: check
        run: | 
          podman --version
